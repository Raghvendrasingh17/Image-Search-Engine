// //arrayLengths
// //The length property returns the length (size) of an array

var employee=["suraj","anil","ankit","ajay"];
let size=employee.length;
console.log(size);


// //Array toString
// //toString() converts an array to a string of (comma separated) array values


var fruits=["Apple","orange","mango","banana"];
console.log(fruits.toString());

// // Array pop()= removes the last element from an array
var car=["Swift","XUV700","Scorpio","Thar"];
car.pop();
console.table(car);


// //Array push()=adds a new element to an array (at the end )

var student=["ajay","hariom","Rahul","Pranay"];
student.push("Anish");
console.table(student);

// //Array shift()=removes the first array element and "shifts" all other elements to a lower index
var mobile=["redmi","poco","techno","samsung"];
mobile.shift();
console.log(mobile);

// //array unshift()=add a new element to an array (at the Starting)
var laptop=["asus","Dell","Hp","Mac"];
laptop.unshift("lenovo");
console.log(laptop);

// // array Splice()=Removes elements from an array and, if necessary, 
// inserts new elements in their place, returning the deleted elements.
var country =["India","nepal","Brazil","Canada","China"];
// country.splice(1,3);
// console.log(country);


const fruits=[ ]
country.s